"""
自适应轮动策略 — 配置模块

核心思路：自动识别市场状态，在"趋势"和"震荡"间动态切换。

市场状态 → 交易模式：
  牛市（HS300 > MA60+3%）：  → 动量模式（买强势、追趋势）
  震荡（MA60±3%之间）：       → 均值回归模式（买超卖、等回归）
  熊市（HS300 < MA60-5%）：   → 空仓模式（保护本金）

动量模式（牛市用）：
  MOM_WINDOW = 30日收益率排名，选最强

均值回归模式（震荡用）：
  %B超卖 + RSI低位 + 缩量确认，选最超卖

空仓模式（熊市用）：
  全部现金，等待市场转好

这种设计使策略能"牛市追涨、震荡抄底、熊市空仓"，
在任何市场状态下都有合适的应对逻辑。
"""

ETF_POOL = {
    "510050": "上证50ETF（华夏）",
    "510300": "沪深300ETF（华泰柏瑞）",
    "510500": "中证500ETF（南方）",
    "512100": "中证1000ETF（南方）",
    "563000": "中证2000ETF（华夏）",
    "159915": "创业板ETF（易方达）",
    "588000": "科创50ETF（华夏）",
}
ETF_SYMBOLS = list(ETF_POOL.keys())

INITIAL_CAPITAL = 10000
START_DATE = "2024-01-01"
END_DATE = ""

# ============================================================================
# 市场状态识别参数
# ============================================================================
REGIME_MA_PERIOD = 60                     # MA周期（用于判断牛/熊/震荡）
BULL_THRESHOLD = 0.03                     # 超过MA60 +3% = 牛市（动量模式）
BEAR_THRESHOLD = -0.03                    # 低于MA60 -3% = 熊市（空仓模式）
                                            # 中间区域 = 震荡（均值回归模式）

# ============================================================================
# 动量模式参数（牛市用）
# ============================================================================
MOM_WINDOW = 30                           # 动量计算窗口
MOM_MIN_HOLD_DAYS = 5                     # 牛市最小持仓天数
MOM_SWITCH_CONVICTION = 0.03              # 牛市切换置信度（动量分差）
MOM_TOP_N = 1                             # 持有前N名

# ============================================================================
# 均值回归模式参数（震荡用）
# ============================================================================
REV_BB_PERIOD = 20                        # 布林带周期
REV_BB_STD = 2.0                          # 布林带标准差倍数
REV_RSI_PERIOD = 14                       # RSI周期
REV_OVERSOLD_RSI = 35                     # RSI超卖阈值
REV_OVERSOLD_PCT_B = 0.0                 # %B超卖阈值
REV_REVERT_PCT_B = 0.5                   # %B回归了结
REV_REVERT_RSI = 50                       # RSI回归了结
REV_STOP_LOSS_PCT_B = -2.0               # %B止损
REV_MAX_HOLD_DAYS = 15                    # 最大持仓天数
REV_MIN_HOLD_DAYS = 3                     # 最小持仓天数

# ============================================================================
# 交易费用
# ============================================================================
COMMISSION_RATE = 0.0002
SLIPPAGE = 0.0001
TAX_RATE = 0.0
ADJUSTMENT_DAYS = 3

# ============================================================================
# 风控
# ============================================================================
RISK_MODE = "A"
STOP_PROFIT_THRESHOLD = 0.10
STOP_PROFIT_DRAWBACK = 0.05
ATR_PERIOD = 20
ATR_MULTIPLIER = 3.0
MAX_DRAWDOWN = 0.20

# ============================================================================
# 数据路径
# ============================================================================
MARKET_INDEX = "000300"
DB_PATH = "data/etf_daily.db"
OUTPUT_DIR = "strategies/adaptive_rotation/output"
BENCHMARK_SYMBOL = "000300"

"""
多周期动量共振策略 - 配置模块

核心思路：单一窗口动量容易被噪音干扰（短期反弹看起来像趋势），
多周期共振要求短/中/长三期动量同时满足条件才入场。

共振条件：
  - MIN_RESONANCE=3: 短中长期动量全部 > 0 才入场
  - MIN_RESONANCE=2: 至少两期动量 > 0
  - MIN_RESONANCE=1: 退化到普通动量轮动

排名用三期动量的加权平均：score = w1*短 + w2*中 + w3*长
"""

# ============================================================================
# ETF 标的池（7只宽基）
# ============================================================================
ETF_POOL = {
    "510050": "上证50ETF（华夏）",
    "510300": "沪深300ETF（华泰柏瑞）",
    "510500": "中证500ETF（南方）",
    "512100": "中证1000ETF（南方）",
    "563000": "中证2000ETF（华夏）",
    "159915": "创业板ETF（易方达）",
    "588000": "科创50ETF（华夏）",
}
ETF_SYMBOLS = list(ETF_POOL.keys())

# ============================================================================
# 回测参数
# ============================================================================
INITIAL_CAPITAL = 10000
START_DATE = "2024-01-01"
END_DATE = ""

# ============================================================================
# 多周期动量参数（核心！）
# ============================================================================
MOMENTUM_SHORT = 5             # 短期动量窗口
MOMENTUM_MEDIUM = 10           # 中期动量窗口
MOMENTUM_LONG = 20             # 长期动量窗口（主信号）
MIN_RESONANCE = 3              # 最小共振数：需要几期动量同时>0
                               # 3=全共振(最严格), 2=两期共振, 1=退化到普通
SCORE_WEIGHTS = (0.2, 0.3, 0.5)  # 短中长权重（长周期权重最大）

# ============================================================================
# 交易费用
# ============================================================================
COMMISSION_RATE = 0.0002
SLIPPAGE = 0.0001
TAX_RATE = 0.0
MIN_COMMISSION = 0.0
IMPACT_COST_COEF = 0.1
ADJUSTMENT_DAYS = 5

# ============================================================================
# 切换控制
# ============================================================================
MIN_SWITCH_CONVICTION = 0.03
SHORT_TERM_MOMENTUM_CHECK = False  # 不需要（多周期共振本身就是确认）
MIN_HOLD_DAYS = 10
TOP_N = 1

# ============================================================================
# 风控
# ============================================================================
STOP_PROFIT_THRESHOLD = 0.10
STOP_PROFIT_DRAWBACK = 0.05
ATR_PERIOD = 20
ATR_MULTIPLIER = 4.0
MAX_DRAWDOWN = 0.15
RISK_MODE = "A"

# ============================================================================
# 数据路径
# ============================================================================
DB_PATH = "data/etf_daily.db"
BENCHMARK_SYMBOL = "000300"
OUTPUT_DIR = "strategies/multi_period_momentum/output"

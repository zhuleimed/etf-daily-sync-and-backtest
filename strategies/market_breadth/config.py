"""
市场宽度择时策略 - 配置模块

核心思路：在动量轮动之上加一层"市场宽度"择时。
宽度 = 价格>MA(N)的ETF占比。

宽度 > 70% → 强市，全力动量轮动
宽度 30%-70% → 中性，半仓或空仓
宽度 < 30% → 弱市，全部空仓

与波动率过滤的区别：
  - 波动率过滤：衡量"趋势是否清晰"（高波动=趋势不可信）
  - 宽度择时：衡量"市场参与度"（多数ETF都在涨=真牛市，少数涨=假行情）

两者从不同的维度判断市场状态，互为补充。
"""

# ============================================================================
# ETF 标的池（7只宽基，与动量轮动一致）
# ============================================================================
ETF_POOL = {
    "510050": "上证50ETF（华夏）",
    "510300": "沪深300ETF（华泰柏瑞）",
    "510500": "中证500ETF（南方）",
    "512100": "中证1000ETF（南方）",
    "563000": "中证2000ETF（华夏）",
    "159915": "创业板ETF（易方达）",
    "588000": "科创50ETF（华夏）",
}
ETF_SYMBOLS = list(ETF_POOL.keys())

# ============================================================================
# 回测参数
# ============================================================================
INITIAL_CAPITAL = 10000
START_DATE = "2024-01-01"
END_DATE = ""

# ============================================================================
# 动量信号参数（与纯动量一致，但宽度过滤会决定是否执行）
# ============================================================================
MOMENTUM_WINDOW = 20
DYNAMIC_WINDOW_ENABLED = False
TOP_N = 1

# ============================================================================
# 市场宽度参数（核心！）
# ============================================================================
BREADTH_MA_PERIOD = 20         # 计算宽度的均线周期
                               # 20日=短期趋势，60日=中期趋势
BREADTH_STRONG = 0.70          # 强市阈值（网格搜索最优#2：70%）
BREADTH_WEAK = 0.30            # 弱市阈值（网格搜索最优#2：30%）
NEUTRAL_MODE = "cash"          # 中性市处理："cash"=空仓, "half"=半仓

# ============================================================================
# 交易费用
# ============================================================================
COMMISSION_RATE = 0.0002
SLIPPAGE = 0.0001
TAX_RATE = 0.0
MIN_COMMISSION = 0.0
IMPACT_COST_COEF = 0.1
ADJUSTMENT_DAYS = 5

# ============================================================================
# 切换控制
# ============================================================================
MIN_SWITCH_CONVICTION = 0.03
SHORT_TERM_MOMENTUM_CHECK = True
MIN_HOLD_DAYS = 10

# ============================================================================
# 风控
# ============================================================================
STOP_PROFIT_THRESHOLD = 0.10
STOP_PROFIT_DRAWBACK = 0.05
ATR_PERIOD = 20
ATR_MULTIPLIER = 4.0
MAX_DRAWDOWN = 0.15
RISK_MODE = "A"                # 宽度择时本身就是风控，不再叠加

# ============================================================================
# 数据路径
# ============================================================================
DB_PATH = "data/etf_daily.db"
BENCHMARK_SYMBOL = "000300"
OUTPUT_DIR = "strategies/market_breadth/output"

"""资产配置策略 — 配置模块

核心思路（机制完全独立于动量轮动）：
  不押注单一最强 ETF，而是多资产分散 + 月度再平衡——
  用低相关资产（宽基/债券/黄金/跨境/货币）构建稳健组合，
  目标不是高收益而是低回撤（与动量策略形成机制互补）。

上班族友好设计：月度调仓（每月 1 次），20:00 管线自动计算权重+推送，
收盘后无需任何手动操作。

四种配置方法对比：
  1. 恒定比例   — 固定权重（如 40/25/15/10/10）
  2. 风险平价   — 权重 ∝ 1/波动率（让各资产风险贡献相等）
  3. 目标波动率 — 组合年化波动率目标 8%，超出部分转入货币
  4. 等权(1/N) — 基准

数据需求：仅 OHLCV（波动率/收益率即可，无估值依赖）。
"""

# ═══ 资产池（低相关类别代表，2019-03 起数据已验证） ═══
ASSETS = {
    "510300": "沪深300(宽基)",
    "511010": "国债ETF(债券)",
    "518880": "黄金ETF(避险)",
    "513100": "纳指ETF(跨境)",
    "159985": "豆粕ETF(商品)",
    "511880": "银华日利(货币)",
}

# 恒定比例权重（与 ASSETS 顺序一致；货币作为安全垫）
CONSTANT_WEIGHTS = {
    "510300": 0.40,
    "511010": 0.25,
    "518880": 0.15,
    "513100": 0.10,
    "159985": 0.00,
    "511880": 0.10,
}

# ═══ 参数 ═══
INITIAL_CAPITAL = 10000.0
VOL_WINDOW = 60              # 波动率计算窗口（交易日，约 3 个月）
ANNUAL_FACTOR = 252
TARGET_VOL = 0.12            # 目标波动率（年化 12%；8% 太保守组合钝化）
MAX_WEIGHT = 0.25            # 单资产权重上限（防债券/货币垄断——初版风险平价失效根因）
REBALANCE_FREQ = "monthly"   # 月度再平衡
START_DATE = "2021-01-01"    # 回测起点（风险平价需波动率预热）

# 成本（与 019 全项目一致）
COMMISSION = 0.0002          # 佣金万2（双向）
SLIPPAGE = 0.0001            # 滑点万1

DB_PATH = "data/etf_daily.db"
